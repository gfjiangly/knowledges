```bash
# 不更新源可能会出现：Unable to locate package错误，最好还要更换源
apt update

# ssh-server
apt install openssh-server

# nano
apt install nano

# git
apt install git

# ping curl
apt install iputils-ping curl

# gcc
apt install gcc

# g++, 必须在安装完gcc后
apt install build-essential

# cmake
apt install cmake

# opencv
sudo apt-get install libopencv-dev python-opencv
```

